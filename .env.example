# Optional: override the default local SQLite path.
# RESIDENCY_SCHEDULER_DB=data/residency_scheduler.sqlite

# Optional: used by callers that wire Google Calendar publishing.
# Keep the credentials file outside git.
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/google-oauth-credentials.json
# GOOGLE_CALENDAR_TIME_ZONE=America/Los_Angeles
